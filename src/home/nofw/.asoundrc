# GStreamer video plugins in Midori fail without access
# to audio devices. An alternative to disabling audio in
# ALSA is temporairly granting playback access:
#   setfacl -m u:nofw:rw /dev/snd/{control*,pcm*p}
#   setfacl -x u:nofw    /dev/snd/{control*,pcm*p}
# Playback is too fast: (#404115)

ctl.!default {
    type null
}

pcm.!default {
    type null
}
