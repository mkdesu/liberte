--- /etc/init.d/hdparm	2010-01-05 03:27:31.000000000 +0000
+++ /etc/init.d/hdparm.new	2010-03-20 15:40:43.000000000 +0000
@@ -33,8 +33,8 @@
 		local orgdevice=$(readlink -f "${device}")
 		if [ -b "${orgdevice}" ] ; then
 			ebegin "Running hdparm on ${device}"
-			hdparm ${args} "${device}" > /dev/null
-			eend $?
+			hdparm ${args} "${device}" 1>/dev/null 2>&1
+			eend 0
 		fi
 	fi
 }
