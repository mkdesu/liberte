# hda_args="-d1 -X66"
# disc1_args="-d1"
# cdrom0_args="-d1"

# Enable DMA on PATA drives
pata_all_args="-d1"

# SATA drives
sata_all_args=""

# Set acoustic management to most quiet.
# Also try to set standby timeout to 5min, since
# HDDs will likely not be used at all.
# (both will typically fail on some drives)
all_args="-M 128 -S 60"
