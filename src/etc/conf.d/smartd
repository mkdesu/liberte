# -q never: don't fail if no SMART-capable devices
# -i [s]: check interval (low, but have -n standby in smartd.conf),
#         must be >> LM_*_HD_IDLE_TIMEOUT_SECONDS in laptop-mode.conf
SMARTD_OPTS="-q never -i 360"
